language: python
python:
  - "3.6"
cache: pip
install:
  - pip install pytest
  - pip install pytest-cov
  #- pip install coveralls
  - pip install codecov
  - pip install -r requirements.txt
script:
  - pytest --cov=test/
after_success:
  #- coveralls
  - codecov
 